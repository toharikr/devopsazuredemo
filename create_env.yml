- hosts: localhost
  tasks:
  
      - install ansible role
        command: ansible-galaxy install azure.aks
        
      - include_role:
           name: azure.aks
        vars:
           monitoring: yes
           name: akscluster
           resource_group: aksroletest
